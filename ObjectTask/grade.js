const student = {
     name: "Amaka", 
     score: 75 
};

    if(student.score >= 90){
     console.log(student.name + "got A")
    }
    else if(student.score <90 && student.score >=80){
     console.log(student.name + " got B")
    }
    else if(student.score <80 && student.score >=70){
     console.log(student.name + " got B")
    }
    else{
    console.log(student.name + " got F")
    }
    
    
